# Ejecutar npm install sobre el directorio del proyecto (si se obtiene algun error, eliminar package-lock.json y volver a intentar)
# Hacer correr la api 
# Ejecutar npm star sobre el directorio del proyecto
